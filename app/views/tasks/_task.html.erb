<div class="col-sm-12">
	<div class="well">
		<h4><%= link_to task.description, task,  remote: true, method: :delete, data: { confirm: 'Are you sure?' } %></h4>
		<p><%= task.deadline %></p>
		<p class="pull-right">
			<%= link_to "Edit", edit_task_path(task), remote: true, class: "btn btn-sm btn-info edit-btn" %>
		</p>
	</div>
</div>